{
  "name": "lengthylogger",
  "version": "1.0.0",
  "description": "a console.log alternative that tells you where you log is being called from (so you can delete it when you are finished with it)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2",
    "string": "^3.3.3"
  },
  "devDependencies": {
    "mocha": "^4.0.1"
  }
}
